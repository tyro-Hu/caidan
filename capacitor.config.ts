import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.hujiankang.caipu",
  appName: "贝贝点菜",
  webDir: "out",
  server: {
    androidScheme: "https",
  },
};

export default config;
