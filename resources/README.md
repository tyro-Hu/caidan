Place native splash and icon source files here if you want to replace the default Capacitor assets.

Recommended next step:
- add a 1024x1024 PNG as `resources/icon.png`
- add a 2732x2732 PNG as `resources/splash.png`

Then generate native assets with a tool such as `@capacitor/assets`.
